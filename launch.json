{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch API",
      "url": "http://localhost:3000/users",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
